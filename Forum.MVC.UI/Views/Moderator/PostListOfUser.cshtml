@model IEnumerable<Forum.MVC.UI.Models.Posts>
<html>
<head>
</head>
<body>

    <table align="center" id="SearchByPost" width="100%" border="1" cellspacing="0" border="1" cellpadding="5" class="table table-striped table-bordered table-hover">
        <thead>
            <tr class="w3-green">
                <th>Post</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Title</td>
                    <td><a href="/Moderator/PostDetails/@item.Id">Open</a></td>
                </tr>
            }
        </tbody>
    </table>
    <script>
        $(document).ready(function () {
            $('#SearchByPost').dataTable({
                "bProcessing": true,
                "bInfo": false
            });
            $('.teachertable_length').addClass('bs-select');
        });
    </script>
</body>
</html>


